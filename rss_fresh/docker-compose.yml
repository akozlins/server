#

x-service: &service
  logging: { options: { max-size: "1m" } }
  restart: "always"
  stop_grace_period: "5s"

services:
  php:
    << : *service
    build: { context: ".", dockerfile: "../rss_tt/Dockerfile.php" }
    command: [ "/run.sh", ]
    networks: [ "default", "inet", ]
    volumes:
      - "./run.sh:/run.sh:ro"
      - "./.local:/var/www"

  nginx:
    << : *service
    image: "nginx:alpine"
    depends_on: [ "php", ]
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${ROUTE:?}.middlewares=authelia@docker"
      - "traefik.http.routers.${ROUTE:?}.rule=PathPrefix(`/${ROUTE:?}`)"
      - "traefik.http.services.${ROUTE:?}.loadbalancer.server.port=8080"
    networks: [ "default", "traefik", ]
#    ports:[ "127.0.0.1:8080:8080", ]
    volumes:
      - "./nginx.conf:/etc/nginx/nginx.conf:ro"
      - "./.local:/var/www"

networks:
  default: { internal: true }
  inet: { external: true }
  traefik: { external: true }
