#

:80 {
    root * /opt/html

    templates
    file_server

    log {
        output stdout
    }
}

:8080 {
    @whoami path /whoami
    handle @whoami {
        reverse_proxy http://whoami:80
    }

    handle {
        redir https://{host}{uri}
    }

    log {
        output stdout
    }
}
