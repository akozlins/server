#

version: "3.9"

x-service:
  &service
  env_file: .env
  logging: { options: { max-size: "1m" } }
  restart: "always"
  stop_grace_period: "5s"

services:

  nginx:
    << : *service
    image: "nginx:alpine"
#    command: [nginx-debug, '-g', 'daemon off;']
    depends_on: [ "traefik" ]
    ports:
      - "0.0.0.0:80:80"
      - "0.0.0.0:443:8443"
#      - "[::]:80:80"
#      - "[::]:443:8443"
    volumes:
      - "./nginx.conf:/etc/nginx/nginx.conf:ro"
      - "./conf.d:/etc/nginx/templates:ro"
      - "./.cache/logs:/etc/nginx/logs:rw"
      - "/opt/html:/opt/html:ro"
      - "/etc/letsencrypt:/etc/letsencrypt:ro"
#      - "./${DOMAIN:?}:/etc/letsencrypt/live/${DOMAIN:?}:ro"
      - "socat:/run/socat:rw"
    healthcheck:
      test: "curl --fail http://127.0.0.1:80 || exit 1"

  # create unix sockets that redirect to 127.0.0.1:$port on host network
  socat:
    << : *service
    image: "alpine"
    command: sh -c "set -euf
        && ( test -x /usr/bin/bash || apk --no-cache add bash )
        && ( test -x /usr/bin/socat || apk --no-cache add socat )
        && exec /etc/socat/run.sh 1022 1443 2580 8008 8053 8080 9080"
    network_mode: "host"
    volumes:
      - "socat:/run/socat:rw"
      - ./socat:/etc/socat:ro

  sslh:
    << : *service
    build: "./sslh"
    command: --listen=127.0.0.1:1443 --ssh=127.0.0.1:22 --openvpn=127.0.0.1:1194 --anyprot=127.0.0.1:1194 --on-timeout=anyprot
    network_mode: "host"

  traefik:
    << : *service
    image: "traefik:latest"
#    ports:
#      - "127.0.0.1:80:80"
#      - "127.0.0.1:8080:8080"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "./traefik.yml:/etc/traefik/traefik.yml"
    labels:
      - "traefik.enable=false"
      - "traefik.http.services.traefik.loadbalancer.server.port=8080"
      - "traefik.http.routers.traefik.service=api@internal"
      - "traefik.http.routers.traefik.rule=(PathPrefix(`/api`) || PathPrefix(`/dashboard`))"

volumes:
  socat:
    driver_opts:
      type: tmpfs
      device: tmpfs

networks:
  default:
    name: server
    external: true
