#

http:
  middlewares:
    auth:
      basicauth:
       users:
       # `htpasswd -nb -B username password | cut -d ":" -f 2`
  routers:
    test-router:
      rule: "Host(`test.{{env "DOMAIN"}}`)"
      service: "test-service"
      tls: true
  serversTransports:
    test-transport:
      serverName: "test.example.com"
  services:
    test-service:
      loadBalancer:
        servers:
        - url: "https://10.8.0.1"
        serversTransport: "test-transport"
